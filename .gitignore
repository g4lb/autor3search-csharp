# Local design notes, plans and run logs. Not part of the shipped tool.
docs/

# Harness output inside a repository under optimization.
.autor3search/
results.tsv
run.log

# .NET build output.
bin/
obj/
artifacts/
nupkg/
*.user

# Test result files. CI asks for a trx logger to read the suite's test count from;
# running that same command locally would otherwise leave committable artifacts.
TestResults/
*.trx

# SDD controller workspace (scratch)
.superpowers/
